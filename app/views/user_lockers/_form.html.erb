<%= form_with(model: user_locker, local: true) do |form| %>
  <% if user_locker.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_locker.errors.count, "error") %> prohibited this user_locker from being saved:</h2>

      <ul>
      <% user_locker.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id, id: :user_locker_user_id %>
  </div>

  <div class="field">
    <%= form.label :locker_id %>
    <%= form.text_field :locker_id, id: :user_locker_locker_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
